setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE
build:
  docker:
    worker: Dockerfile
  config:
    POSTGRES_URI: tosibosi
release:
  command:
    - ./migrate -database "postgres://user:pass@127.0.0.1:5432/project?sslmode=disable" -path ./migrations up
  image: worker
